<!DOCTYPE html>
<html lang="es">

{{>head}}

<body>
  <header class="bg-primary text-white">
    <div class="container d-flex justify-content-between align-items-center">
      <h1 class="my-4">Mi dashboard</h1>
      <p class="d-flex justify-content-end align-items-center">Bienvenido, {{usuario.name}}</p>
    </div>
  </header>

  <div class="container-fluid">
    <div class="row">
      <div class="col-md-3 bg-light bg-gray d-flex flex-column align-items-start">
        <div class="p-3">
          <h2 class="mb-0">Menú</h2>
        </div>
        <div class="flex-grow-1">
          <ul class="list-group w-100">
            <li class="list-group-item mb-3"><a href="/tablas-publicaciones">Ver todas las publicaciones</a></li>
            <li class="list-group-item mb-3"><a href="/usuarios">Ver todos los usuarios</a></li>
          </ul>
        </div>
      </div>

      <div class="col-md-9">
  <div class="row justify-content-center">
    <div class="col-md-6">
      
      <form action="/editar-usuario/{{id}}" method="post">
        <div class="form-group">
          <label for="name">Nombre</label>
          <input type="text" class="form-control" id="name" name="name" value="{{this.name}}" required>
        </div>
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" class="form-control" id="email" name="email" value="{{email}}" required>
        </div>
        <div class="form-group">
          <label for="tipoRol">Rol</label>
          <input type="text" class="form-control" id="tipoRol" name="tipoRol" value="{{tipoRol}}" required>
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary">Guardar cambios</button>
        </div>
      </form>
      
    </div>
  </div>
</div>
    </div>
  </div>

  {{>footer}}

</body>

</html>
